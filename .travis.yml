language: java
dist: trusty
addons:
  apt:
    packages:
      - oracle-java8-installer
jdk: oraclejdk8
deploy:
  provider: releases
  api_key: "$GH_TOKEN"
  file: "target/App.jar"
  skip_cleanup: true
  on:
    tags: false